import 'package:flutter/material.dart';
class CustomSpacing extends StatelessWidget {
  const CustomSpacing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}
